let fruits = ['apples', 'pears', 'cherries', 'bananas', 'peaches', 'oranges']; //declares an array
let hasApples = fruits.includes('apples');  // returns a boolean value for if the array includes an element called apples
let hasMangoes = fruits.includes('mangoes');  // returns a boolean value for if the array includes an element called mangoes
console.log(hasApples); //logs the boolean, in this case true
console.log(hasMangoes); //logs the boolean, in this case false