function addTwo(num1, num2) { //declares a function, calls it addTwo, and states that two variables are needed
    return num1 + num2; //returns the result of the addition of two numbers to the user, the two numbers being the variables supplied
  }
addTwo(3, 5); // this is outside the function, and the function is called by the function name addTwo, and is executed on the two numbers supplied in the brackets

let result = addTwo(5, 7); //This calls the function and assigns the output of it as the value of the variable named result

